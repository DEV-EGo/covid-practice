var favMovies = [];
var Movies;

// FOR-Loop will repat 3x

for (var i = 1; i < 3; i++) {

    // asking user what is there 1,2,3 fav movie

    favMovies = prompt("what's your # " + i + " favorite movie ?");

    // adding (PUSH) the variable aka answer into favmovie array

    favMovies.push(Movies);
}